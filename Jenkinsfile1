withSonarQubeEnv('MySonar') {
    sh '''
        sonar-scanner \
        -Dsonar.projectKey=First-CI \
        -Dsonar.sources=. \
        -Dsonar.host.url=$SONAR_HOST_URL \
        -Dsonar.login=$SONAR_AUTH_TOKEN
    '''
}
